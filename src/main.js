// 匯入客製化的 SCSS 樣式
import './styles.scss'; 

// 匯入 Bootstrap 的 JavaScript 核心 (用於導覽列收合、Tab 切換等功能)
import * as bootstrap from 'bootstrap'; 

// 專案啟動時的額外 JS 邏輯 (可選)
document.addEventListener('DOMContentLoaded', () => {
    // 範例：導覽列平滑捲動效果 (需自行安裝相關套件或使用原生 JS 實現)
    // 這裡只做一個簡單的控制台輸出
    console.log('文藻小咖啡網站已載入完畢！');
});